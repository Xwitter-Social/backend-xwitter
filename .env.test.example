# Configuração da base utilizada pelos testes de integração fora do Docker
# Ajuste o host (localhost) e o nome do banco conforme sua máquina.
TEST_DATABASE_URL="postgresql://user:password@localhost:5432/twitter_db?schema=test"

# Chave usada para assinar tokens JWT durante os testes
JWT_SECRET="your_jwt_secret_key_here"
